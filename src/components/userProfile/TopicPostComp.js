import React from 'react';import {MdOutlineTopic} from "react-icons/md";import {Link} from "react-router-dom";const TopicPostComp = ({topic}) => {    const date = new Date(topic.lastPostInfo.lastPostTime)    const lastPost = `${date.getFullYear()}:${date.getMonth() < 10 ? '0' + date.getMonth() : date.getMonth()}:${date.getDay() < 10 ? '0' + date.getDay() : date.getDay()}`    const time = `${date.getHours()}:${date.getMinutes() < 10 ? '0' + date.getMinutes() : date.getMinutes()}:${date.getSeconds() < 10 ? '0' + date.getSeconds() : date.getSeconds()}`    return (        <div className='topic'>            <p className='col-7 topic-topic ms-sm-2'><MdOutlineTopic className='me-sm-2'/><Link                to={'/topic/' + topic.id}>{topic.title}</Link></p>            <p className='col-1 topic-replies text-center'>{topic.postsCount}</p>            <div className='col-4 topic-post-info text-center'>                <div>{lastPost + ' ' + time}</div>                <div>{topic.lastPostInfo.lastPostUser}</div>            </div>        </div>    );};export default TopicPostComp;