import React, {useContext} from 'react';import {BsArrowRight, BsBoxArrowInRight} from "react-icons/bs";import {Link} from "react-router-dom";import mainContext from "../../context/mainContext";const Hamburger = ({hamburgerTrigger, setHamburgerTrigger, logout, favoritesCount, notifications}) => {    const {user} = useContext(mainContext)    return (        <div className="toolbar-responsive-container"             style={{display: hamburgerTrigger ? "block" : "none"}}>            <div style={{display: user?.username ? "block" : "none"}}>                {notifications &&                    <Link to="/notifications" onClick={() => setHamburgerTrigger(false)}>                        <div className="responsive-tab-wrap">                            <p>Notifications ({user?.notifications.length})</p>                            <BsArrowRight/>                        </div>                    </Link>                }                <Link to="/create-topic" onClick={() => setHamburgerTrigger(false)}>                    <div className="responsive-tab-wrap">                        <p> Create Topic</p>                        <BsArrowRight/>                    </div>                </Link>                <Link to="/favorites" onClick={() => setHamburgerTrigger(false)}>                    <div className="responsive-tab-wrap">                        <p>Favorites ({favoritesCount})</p>                        <BsArrowRight/>                    </div>                </Link>                <Link to="/user-profile" onClick={() => setHamburgerTrigger(false)}>                    <div className="responsive-tab-wrap">                        <p>User Profile</p>                        <BsArrowRight/>                    </div>                </Link>                <div className="responsive-tab-wrap" onClick={() => {                    setHamburgerTrigger(false);                    logout();                }}>                    <p>Logout</p>                    <BsBoxArrowInRight/>                </div>            </div>        </div>    );};export default Hamburger;